JWT_SECRET=
DATABASE_URL=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_S3_BUCKET=

# === DATABASE (single source of truth) ===
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# === POSTGRES CONTAINER (mirror DB_*) ===
POSTGRES_DB=${DB_NAME}
POSTGRES_USER=${DB_USER}
POSTGRES_PASSWORD=${DB_PASSWORD}

# === APP ===
APP_MODE=
APP_PORT=
ML_API_URL=
